# Phase 2 LangChain interfaces and local CPU/CUDA embedding probe; Python 3.11.
# Install with uv pip install --python .venv/bin/python -r requirements-phase2.txt
# Direct/model dependencies pinned; environment.json records the resolved environment.
-r requirements-phase1.txt
torch==2.5.1
transformers==4.57.6
tokenizers==0.22.2
huggingface-hub==0.36.2
safetensors==0.8.0
numpy==1.26.4

# Compatible with the existing LangChain 0.3 project; no framework upgrade.
langchain-core==0.3.63
langchain-groq==0.2.3
groq==0.13.1
